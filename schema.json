{
  "type": "object",
  "properties": {
    "hintLevel": {
      "description": "Determine production throws or development warnings",
      "enum": ["error", "warning"]
    },
    "env": {
      "description": "Js environment of your bundle code runs",
      "enum": ["es6", "es5"]
    }
  },
  "additionalProperties": false
}
