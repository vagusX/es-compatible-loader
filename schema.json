{
  "type": "object",
  "properties": {
    "mode": {
      "description": "Determine production throws or development hints",
      "enum": ["development", "production"]
    },
    "env": {
      "description": "Js environment of your bundle code runs",
      "enum": ["es6", "es5"]
    }
  },
  "additionalProperties": false
}
